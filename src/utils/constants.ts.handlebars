import { Address, BigDecimal, BigInt } from '@graphprotocol/graph-ts'
import { Hub } from '../types/Hub/Hub'

export const ADDRESS_ZERO = '0x0000000000000000000000000000000000000000'
export const HUB_ADDRESS = '{{address}}'
export const MANAGER_ADDRESS = '{{managerAddress}}'

export const ZERO_BI = BigInt.zero()
export const ONE_BI = BigInt.fromI32(1)
export const ZERO_BD = BigDecimal.zero()
export const ONE_BD = BigDecimal.fromString('1')
export const BI_18 = BigInt.fromI32(18)

export const hubContract = Hub.bind(Address.fromString(HUB_ADDRESS))

export const MAX_TIERS: i32 = {{MAX_TIERS}}

export const USDC_ADDRESS = '{{USDC_ADDRESS}}'
export const WETH_ADDRESS = '{{WETH_ADDRESS}}'

// Limit order types
export const NOT_LIMIT_ORDER: i8 = 0
export const ZERO_FOR_ONE: i8 = 1
export const ONE_FOR_ZERO: i8 = 2

// token where amounts should contribute to tracked volume and liquidity
// usually tokens that many tokens are paired with
export const WHITELIST_TOKENS: string[] = [
  WETH_ADDRESS,
  USDC_ADDRESS,
{{#WHITELIST_TOKENS}}
  '{{.}}',
{{/WHITELIST_TOKENS}}
]

export const STABLE_COINS: string[] = [
  USDC_ADDRESS,
{{#STABLE_COINS}}
  '{{.}}',
{{/STABLE_COINS}}
]
